//@flow
// Data-binding functions

declare export var bind : BindSpecs => Element => BindFunction

type BindFunction
    = Object => void // TODO: What to return?

type BindSpecs
    = Array<BindSpec>

type BindSpec
    = {| query: Query, key: string, attr: string |}
    | {| query: Query, key: string, prop: string |}

type Query = string // TODO: refine this?
