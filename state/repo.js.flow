//@flow

type Repo<S>
  = { merge: Merge<S>, view: View<S> }

type Merge<S> = <K>((S,K) => S) => (K => K)
type View<S> = <K,R>((S,K) => R) => (K => R)

type Clone<S> = S => S

declare export var repo : <S>(Clone<S>) => S => Repo<S>
